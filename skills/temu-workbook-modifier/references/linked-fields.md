# Linked-field closure

Use normalized header names. Verify actual column positions in each workbook.

| Trigger | Required linked review/update |
|---|---|
| D `产品货号` | E variant display, I model value, SKU/model fields, parsed SKC `extCode` according to mode, title/fingerprint index, source→final D manifest, plugin/registry target after certification |
| G `变种属性值一` | E `变种名称`, SKU JSON first `specName`, row-level J source/match, SKC `previewImgUrls`; preserve real color/count/size/material semantics |
| J `预览图` | Exact row `D+G+SKU` positive source lineage, generated/local asset, durable URL, SKC `previewImgUrls == J`; visual review remains separate |
| T1 | Same-D T first URL on all rows, U=T1, approved source/candidate chain, person/task/product fidelity, badge stage if required |
| T carousel | Same-D equality, both delimiter types, T≤10, T4 protected/size, required category feature images, URL safety |
| Category ID | Product attribute JSON parsing, `templatePid` consistency, certified L0xx reference/registry |
| Title | Same-D consistency, body reconstruction, trailing fingerprint policy, global uniqueness/ambiguity audit |
| Description | Same-D consistency, valid rich HTML/URLs, exact product relationship; do not erase valid image HTML |
| Price | SKU source-map coverage, multiplier/rounding, numeric type, all physical rows |
| X package image | Workbook-wide universal majority OSS value, same-D consistency, no `kwcdn` |
| Upload copy | Preserve headers/rows; clear source/shop/timestamp data fields only in upload copy |

Count/quantity/color claims are visual facts. Filename matching, same folder, or JSON equality cannot prove them.

