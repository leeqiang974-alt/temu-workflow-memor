# Project memory map

Read the project `AGENTS.md` first; it is the authoritative durable correction log. Route details as follows:

- T1 generation/reference editing: `$temu-tfirst-reference-edit`, `$product-lifestyle-scene`, and provider-specific runner.
- Workbook image fields and review pages: `$temu-xuanxshop-image-workbook`.
- General title/J/T/U workflow: `$temu-xuanxshop-workflow`.
- OSS upload: project `skills/oss-upload/SKILL.md` when available; never print credentials.
- BitBrowser/Temu page work: `$bitbrowser-temu-automation` and canonical extension path from `AGENTS.md`.
- Final release: `$temu-release-gate`; construction scripts cannot waive it.

When historical rules conflict, prefer the most recent date and the rule whose batch/mode matches the active workbook. Do not carry circular/square badge shape, source library, date-code, or extCode rules across batches without an explicit mode match.

