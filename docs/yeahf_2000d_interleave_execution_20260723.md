# YeahF 2000D 穿插扩容执行约束

## 已冻结输入

- 1711D 底表：`YeahF_合并总表_追加补充144D_L043_20PCS_J回填_20260723.xlsx`
- 底表规模：3161 个物理数据行、54 列、1711 个唯一 D。
- 新增来源：289 个临时 D，仅用于来源追踪，不得把 `0726` 临时编号直接写入最终表。

## 图片重做

- 每个 D 只允许使用已经核验的原稿参考图，不允许使用上一轮生成图作为参考图。
- 产品结构、颜色、数量、五金、支撑、使用逻辑保持不变。
- 强制改变构图尺度、产品位置、相机距离、空间纵深、场景材质、色调与光向。
- 仅在产品族允许时水平镜像；L043、L047、L058、L082、L088、L089、L091 禁止镜像。
- L077 更换宠物；L095 更换花卉；L088/L089 更换架上物品；带柜体环境只改变周围柜体外观，不改变产品。
- 全量视觉审核通过前，禁止 OSS 上传和工作簿写回。

## 2000D 穿插与联动

- 按相同 L0xx，把新增 D 均衡分配到该 L0xx 已存在的日期段。
- 在每个日期段内部把新增组与旧组均匀穿插，不在日期尾、L0xx 尾或文件尾集中追加。
- 对穿插后的完整 D 组重新顺序编号；同一 D 的全部变体行必须连续。
- 所有 54 列整行移动，禁止只移动部分列。
- 建立并冻结两张映射：`旧1711 D -> 最终 D`、`临时289 D -> 最终 D`。
- 按映射解析式更新所有 D 依赖字段和 JSON；禁止全文字符串替换。
- 最终必须满足：2000 个唯一 D、零重复、零断组、零倒序、零 `0726` 临时新增编号残留。

## 发布门禁

- 使用正式 Release Guard。
- J 为逐物理行零信任门禁；非空 URL 或 AC 同步不代表视觉通过。
- T1 使用审核通过的新图；T4 保持同 L0xx 尺寸图槽；U 必须等于 T1。
- L058 必备功能图必须覆盖每个 L058 D。
- 只有重导入、逐格联动、视觉审核与正式证书全部通过，才可称为可提交。
